type: object
properties:
  session_id:
    type: string
    format: uuid
  user:
    type: object
    properties:
      id:
        type: string
        format: uuid
      name:
        type: string
      email:
        type: string
        format: email
