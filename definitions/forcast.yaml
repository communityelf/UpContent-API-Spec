title: forecast
type: object
properties:
  previous:
    type: object
    properties:
      total:
        type: integer
      currency:
        type: string
      date:
        type: string
        format: dateTime
  upcoming:
    type: object
    properties:
      total:
        type: integer
      currency:
        type: string
      date:
        type: string
        format: dateTime
