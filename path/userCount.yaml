get:
  summary: Get number of upcontent users --NEEDS WORK--
  description: |
    Get number of upcontent users
  tags:
    - User
  responses:
    200:
      description: User connected
      schema:
        type: object
        properties:
          user-count:
            type: string
            description: Unique identifier for a user
    default:
      description: Unexpected error
      schema:
        $ref: '../definitions/error.yaml'
