post:
  operationId: create_merge_token
  summary: Create merge token --NEEDS WORK--
  description: Create merge token
  tags:
    - Merge Token
  responses:
    200:
      description: User succesfully created
      schema:
        type: object
        properties:
          merge-token:
            type: string
            format: uuid
            description: --NEEDS WORK--
    403:
      description: User not connected
    default:
      description: Unexpected error
      schema:
        $ref: 'definitions/error.yaml'
