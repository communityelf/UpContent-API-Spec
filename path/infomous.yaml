get:
  summary: Get influence graph
  description: |
    Get influence graph
  tags:
    - Suggestions
  parameters:
    - name: data
      in: body
      required: true
      schema:
        type: object
        properties:
          feed_url:
            type: string
            description: --Needs work--
  responses:
    200:
      description: Infomous graph found
      schema:
        type: object
        properties:
          words:
            type: array
            items:
              type: string
              description: --Needs work--
          sources:
            type: object
            properties:
              attributes:
                type: object
                description: --Needs work--
                additionalProperties: {}
    502:
      description: Unexpected error
    504:
      description: Infomous timed out
    default:
      description: Unexpected error
      schema:
        $ref: 'http://spec.upcontent.com/upc/definitions/error.yaml'
